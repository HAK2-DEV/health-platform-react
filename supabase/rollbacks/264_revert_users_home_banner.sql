-- ============================================================
-- Rollback: 264 - 대시보드 인사 배너 사진 되돌리기
-- 작성일: 2026-09-17
-- 주의:
--   컬럼을 지우면 사용자가 고른 배너 «경로» 기록이 사라진다. 스토리지의 사진 파일 자체는
--   profile-avatars 버킷에 남으므로, 되돌린 뒤 정리하려면 각 사용자 폴더의 home-banner-*.jpg
--   를 따로 지워야 한다(아바타와 같은 폴더라 통째로 지우면 프로필 사진까지 사라진다).
-- ============================================================

ALTER TABLE public.users
  DROP COLUMN IF EXISTS home_banner_path;
