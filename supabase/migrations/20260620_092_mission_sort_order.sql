-- ============================================================
-- Migration: 092 - 미션 정렬 순서 (운영자 수동 정렬)
-- 작성일: 2026-06-20
-- 설명:
--   미션 관리자 작업 페이지에서 운영자가 미션 순서를 위/아래로 조정할 수 있도록
--   정렬용 컬럼 추가. NULL → 정렬 안 된 미션 (목록에서 created_at 순으로 뒤에 배치).
--   클라이언트가 정렬 변경 시 전체 미션에 0,1,2... 순번을 재할당한다.
--   기존 미션 영향 없음(하위호환) — 코드는 컬럼이 있을 때만 정렬/저장.
--
-- 복구: ALTER TABLE public.missions DROP COLUMN IF EXISTS sort_order;
-- ============================================================

ALTER TABLE public.missions
  ADD COLUMN IF NOT EXISTS sort_order INTEGER;
