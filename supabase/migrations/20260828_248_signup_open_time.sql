-- ============================================================
-- Migration: 248 - 클래스 신청 개방 "시각" 지정
-- 작성일: 2026-08-28
-- 설명:
--   245/246 으로 "시작 N일 전부터" 는 정할 수 있게 됐지만, 개방 **시각**은 고를 수 없었다.
--   현재 계산은 `클래스 시작시각 - N일` 이라 개방 시각이 클래스 시작 시각과 같아진다:
--     · 저녁 7시 클래스 + 3일 전  → 3일 전 **저녁 7시**에야 열림
--       (운영자는 "그날 아침부터" 로 이해하는데 하루를 통째로 기다리는 느낌)
--     · 오전 10시 클래스와 저녁 7시 클래스의 개방 시각이 **제각각** → 선착순이면 불공평
--   그래서 "N일 전 + 지정 시각(예: 오전 9시)" 로 못박을 수 있게 한다.
--
--   programs.class_signup_open_time : 프로그램 기본 개방 시각
--   sessions.signup_open_time       : 이 클래스만 다르게 (NULL = 프로그램 기본값 따름)
--
--   둘 다 NULL 이면 **기존 동작 그대로**(클래스 시작 시각 기준) — 245/246 과 같은 2단 구조.
--   시각만 정하고 lead_days 가 0/NULL(항상 열림)이면 게이팅 자체가 없으므로 무의미하다
--   → 클라이언트에서 lead_days > 0 일 때만 시각 입력을 노출한다.
--
--   TIME 타입(시:분). 타임존은 저장하지 않는다 — 앱이 KST 단일 사용자층이고,
--   클라이언트가 로컬 시각으로 해석하므로 "오전 9시" 가 곧 KST 09:00 이다.
--
-- 하위호환:
--   컬럼 추가만. 기존 행은 NULL → 지금과 동일하게 동작한다.
--   이 마이그레이션만 먼저 적용해도 기존 코드는 새 컬럼을 읽지 않으므로 무영향.
--
-- 복구:
--   supabase/rollbacks/248_revert_signup_open_time.sql 수동 실행.
-- ============================================================

ALTER TABLE public.programs
  ADD COLUMN IF NOT EXISTS class_signup_open_time TIME;

ALTER TABLE public.sessions
  ADD COLUMN IF NOT EXISTS signup_open_time TIME;
