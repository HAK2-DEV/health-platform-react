-- ============================================================
-- Migration: 261 - 건강 관련 정보(민감정보) 별도 동의 시각
-- 작성일: 2026-09-15
-- 설명:
--   개인정보 보호법 제23조 — 건강 정보는 민감정보라 «별도 동의»가 필요하다.
--   가입 동의(약관·개인정보·만14세·마케팅)에는 없어, 건강 정보를 «처음 입력하는 시점»
--   (기분 체크·설문 응답·미션 수치 입력·금연 프로그램 참여)에 1회 동의를 받아 여기에 기록한다.
--   가입 폼 체크박스가 아니라 기능 진입 시 동의로 설계한 이유: 이미 가입한 회원(112명)도
--   자연스럽게 동의를 거치게 하기 위함(가입 폼은 기존 회원이 다시 지나가지 않는다).
--
--   NULL = 아직 동의하지 않음(기본). 값 = 동의한 시각. 갱신은 본인 행 UPDATE 정책(003)으로 가능.
--   하위호환: nullable 컬럼 추가뿐 — 기존 코드·행에 영향 없음.
--
-- 복구:
--   ALTER TABLE public.users DROP COLUMN IF EXISTS agreed_health_at;
-- ============================================================

ALTER TABLE public.users
  ADD COLUMN IF NOT EXISTS agreed_health_at timestamptz;

COMMENT ON COLUMN public.users.agreed_health_at IS
  '건강 관련 정보(민감정보) 수집·이용 별도 동의 시각. NULL=미동의. 개인정보처리방침 2조·개인정보 보호법 23조.';
