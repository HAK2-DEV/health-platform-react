# 제보·버그 로그 (도담)

> **원칙: 제보가 들어오면 «즉시» 한 줄 남긴다. 고치는 건 그다음이다.**
> 나중에 기억으로 복원하려 하면 반드시 빈약해진다. 이 로그는 두 군데에 쓰인다.
> ① 개발 트리아지 ② 프로덕션 액세스 신청서의 「의견 요약」([PLAY_STORE_RELEASE.md](PLAY_STORE_RELEASE.md) 6-0(b)).

## 접수할 때 반드시 같이 묻는 것

코드를 오래 노려보는 것보다 **기기와 재현 조건을 먼저 묻는 편이 훨씬 빨랐다**(2026-09-17 두 번 확인).

1. **기기·OS** — 노트9(안드10)인지 S25인지에 따라 원인이 완전히 달라진다.
2. **화면 버전** — 계정 설정 맨 아래 「앱/웹 · 화면 버전 XXXX · 글자 ×N」 줄을 캡처해 달라고 한다.
   업데이트 직후 첫 실행은 서비스워커 때문에 **옛 번들이 도는 일이 있어**, 이 줄이 없으면 이미 고친 걸 또 쫓게 된다.
3. **무엇을 하다가** — 화면 이동 순서 그대로. 「사진 고르고 돌아오니」처럼 앱이 잠깐 백그라운드로 간 정황이 결정적일 때가 많다.
4. **스크린샷** — 글자 잘림·버튼 가림은 말로 설명하기 어렵다.

## 상태 표기

`접수` → `재현` → `수정` → `배포` → `확인`(제보자가 사라졌다고 확인) / `재현실패` / `보류`

**「수정」과 「확인」은 다르다.** 재현하지 못한 채 고친 것은 `배포` 까지만 적고,
제보자가 다시 확인해 주기 전에는 `확인` 으로 올리지 않는다.

---

## 진행 중

| # | 날짜 | 제보 경로 | 기기·화면버전 | 증상 (제보 원문) | 상태 | 조치 |
|---|---|---|---|---|---|---|
| 15 | 2026-09-18 | **내부 발견** (Play 심사용 계정 준비 중) | 서버(RLS) — 기기 무관 | 운영자가 «자기 프로그램»에서 미션을 기록하면 제출이 거부된다. 화면에는 제출 버튼이 «보인다» | 접수 | 원인 확정 — 클라와 DB 판단이 어긋난다. `MissionVerifyPage:250` 은 `canVerify = isOwner \|\| 참여자ACTIVE` 라 운영자에게 버튼을 주는데, `verifications` INSERT 정책(마이그 018)은 **ACTIVE 참여자 행만** 허용하고 owner 조항이 없다 → 403. ⚠️ 403 화면을 직접 재현하진 않았고 정책 원문으로 확정. 참여자 행을 넣자 제출이 201 로 성공한 것이 방증. 수정 방향 2택: ①정책에 owner 추가 ②운영자에겐 버튼 숨김 — 제품 결정 필요 |
| 16 | 2026-09-20 | **테스터 제보(도영)** | **S24+ · One UI 8.5 · 안드16** · v46(트랙 최신) · 앱 | 「갤럭시는 아래에 뒤로가기·홈버튼이 있어서 대응해야 할 듯. 이렇게 겹침」 — 인증 제출 화면 하단 「취소 · 인증 제출」이 3버튼 내비게이션 바에 가림 | 수정 | ① safe-area 4곳(`0b458eb`·`55af2b8`) → **웹 배포 완료**, v47 에 담김 ② 근본 원인은 안드16이 엣지투엣지 해제를 무시하는 것 → **v48 에서 EdgeToEdge 정식 이행**(`00e2c1d`). 안드16 에뮬·노트9 양쪽 검증 완료. ⚠️ 제보자 확인 전까지 「확인」으로 올리지 않는다 |
| 14 | 2026-09-18 | **본인 체감** | 노트9 PWA | 「pwa랑 네이티브 앱 조금 느려진 것 같다 — 화면 이동(탭·프로그램 열기), 며칠 전부터 조금씩」 | 보류 | 실측으로 원인은 «동시 요청 폭주» 확정(이미지 43개+Supabase 7개 동시). `Icon3D` 지연 로딩 배포했으나 **재측정에서 분포가 그대로 = 실효 미확인**. 본인 결정으로 보류 — **버그 게시판에 같은 제보가 들어오면 재개**(기기 정보가 자동으로 붙어 조건이 명확해짐). 상세: `project_perf_program_detail_2026-09-18` |
| 13 | 2026-09-18 | **노트9 실기기** | 노트9(안드10) · v46 | 「취소·다음 버튼이 (화면) 위에 있는 건 마음에 들지 않는다」 — 키보드가 뜨면 카드가 52vh 로 갇혀 화면 위쪽에 붙고, 버튼이 화면 중간에 뜬다 | 보류 | 높이를 모르는 구형 안드에서 버튼을 확실히 보이게 하려는 설계의 부작용. 키보드 높이를 알 수 있으면 카드를 키보드 바로 위에 앉힐 수 있다 — 별도 주제로 다룰 것 |
| 12 | 2026-09-17 | **노트9 실기기** | 노트9(안드10) · v46 | 「글쓰기에서 갤럭시 뒤로가기로 키보드 내리면 취소·게시가 안 내려가고, 사진첨부 모달도 안 나옴. 빈 곳 클릭하면 나옴」 | 보류 | 안드로이드가 뒤로가기를 IME 에서 삼켜 앱이 감지 못 함. 「입력칸 밖 첫 터치」 복귀가 현재 설계([[lib/nativeKeyboard]]). 다른 신호를 찾기 전엔 구조적 한계 |

---

## 처리 완료

> **v46 (1.2.19) — 2026-09-18 Play 비공개 테스트 업로드 완료.** 아래 9·10·11 이 이 빌드에 담겼다.
> 같은 날 `mvp-v2` 에도 배포되어 **PWA 사용자에게도 반영**됐다(커밋 `5ff2751` 까지).
>
> 아래 초기 항목은 **커밋 기록에서 되살린 것**이라 제보자·기기가 비어 있다.
> 앞으로 들어오는 제보는 접수 시점에 위 4가지를 채워 넣는다.

| # | 날짜 | 제보 경로 | 기기·화면버전 | 증상 | 상태 | 조치 |
|---|---|---|---|---|---|---|
| 11 | 2026-09-18 | **노트9 실기기** | 노트9(안드10) · v46 | 「제목 수정하고 미션 설명 칸을 누르고 싶은데 그 위를 취소·다음 버튼이 막고 있다. 모달을 아래로 드래그해야 누를 수 있다」 | 확인 | ① 보정이 `[lift]` 에만 걸려 칸 간 이동엔 안 돌았다 → 훅·공용 Modal 에 `focusin` 보정(본인: 누르면 버튼이 내려감) ② 「누르기 전」 가림은 sticky 의 본질 → MissionCreateModal 을 flex 로. 본인 확인: **「그나마 좀 낫다」** (잔여 불만은 13번) · `9df121f` |
| 10 | 2026-09-17 | **노트9 실기기** | 노트9(안드10) · v46 | 키보드 뜬 뒤 «두 번째» 입력칸으로 옮겨가면 그 칸이 버튼에 덮인 채로 남음 | 확인 | 보정이 lift 변화에만 걸려 한 번만 돌았다 → `focusin` 으로 칸 간 이동도 보정 · `db94df0` |
| 9 | 2026-09-17 | **노트9 실기기** | 노트9(안드10) · v46 | 키보드가 뜨면 「취소·일괄 응원 보내기」 버튼이 입력칸을 덮음 (응원 보내기) | 확인 | `useKeyboardOverlay` 에 접힘 후 포커스 입력칸 끌어오기 · uiautomator 실측 여유 116px · 본인 확인 · `6606aff` |
| 8 | 2026-09-17 | 커밋 기록 | 구형 안드 | 키보드가 뜨면 모달 21곳의 액션 버튼이 가림 | 배포 | `6e885e2` · v46 |
| 7 | 2026-09-17 | 커밋 기록 | 미기록 | 사진 편집을 저장하면 글쓰기 모달까지 닫힘 (뒤로가기 정리의 경쟁 조건) | 배포 | `e219191` · v46 |
| 6 | 2026-09-17 | 커밋 기록 | 미기록 | 초안이 sessionStorage 라 앱이 죽으면 함께 사라짐 | 배포 | `4433e3e` |
| 5 | 2026-09-17 | 커밋 기록 | 미기록 | 사진 첨부 3종 — 용량 상한 불일치·HEIC 변환 표시·크롭 저장 버튼 가림 | 배포 | `dfaaa32` |
| 4 | 2026-09-17 | **16전비 실사용자** | 미기록 | 사진 고르는 사이 앱이 회수돼 쓰던 글·입력이 날아감 | 배포 | `b388180` |
| 3 | 2026-09-17 | 커밋 기록 | 구형 안드 | 글쓰기·글 수정 모달에서 「취소·수정」 버튼이 안 보임 | 배포 | `6819dd0` |
| 2 | 2026-09-16 | **노트9 제보** | 노트9(안드10) | 마법사에서 대표사진 고르고 오면 1단계로 초기화됨 | 배포 | v42 방어 · 에뮬 재현 실패 (재발 시 인트로 화면이 보였는지 물을 것) |
| 1 | 2026-09-15 | **실기기 실측** | 노트9 · 글자 ×1.3 | 폰 글자 크기 설정이 웹뷰에 곱해져 배너·버튼이 넘침 | 확인 | v41 · 1.15배 상한 |

---

## 관련 메모

- 검증 없이 「고쳤다」고 말하지 않기 — `feedback_verify_before_claiming_2026-09-17`
- 네이티브 서비스워커 캐시(업데이트 첫 실행은 옛 JS) — `feedback_native_sw_cache_uninstall`
- 사진 입력은 `prepareImageFile` 필수(HEIC) — `feedback_image_input_prepare`
- 노트9 원격 디버깅 방법 — `reference_note9_pwa_cdp_2026-09-17`
